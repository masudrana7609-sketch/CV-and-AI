# CV-and-AI
This project uses a YOLO-based deep learning model to automatically detect bone fractures from X-ray images. The model is trained on a labeled dataset containing different types of fractures, allowing it to identify and localize abnormalities using bounding boxes.
